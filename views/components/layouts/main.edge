<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title ? `${title} | ` : "" }}El Jardín de Al</title>
    <link rel="icon"
        href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🍂</text></svg>" />
    {{{ $.css("pages/common") }}}
    {{{ await $slots.styles?.() ?? "" }}}

    {{{ $.js("pages/common") }}}
    {{{ await $slots.scripts?.() ?? "" }}}
</head>

<body>
    @!ui.header()
    <view-partial>
        <main class="container">
            {{{ await $slots.main() }}}
        </main>
    </view-partial>
    @!ui.footer()
</body>

</html>