@layouts.main({ title: "Explora" })
@slot('styles')
{{{ $.css('pages/home') }}}
@end

<section class="hero">
    <h1>Busca por categoría</h1>
</section>
<ul role="list" class="grid">
    @each(tag in tags)
    <li class="card">
        <a href="/explore/{{ tag }}/">
            {{ titleCase(tag).replaceAll("-", " ") }}
        </a>
    </li>
    @end
</ul>
@end